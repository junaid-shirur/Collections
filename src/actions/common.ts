import { commanAction } from "./types";

export namespace Action {
    export enum Method {
        test
    }

    // const actionCreator: (action: any) => AppDispatch = (method) => {
    //     let dispatch =  useAppDispatch()
    //     return dispatch(method)
    // }


    // export const test = (payload = '') => actionCreator(Method.test)

    // export const actionCreators = {
    //     test
    // }
}